# -*- mode: snippet -*-
# name: react_functional_component
# key: rfc
# --
import React from 'react';

function ${1:`(capitalize (file-name-sans-extension (buffer-name)))`}(${2:props}) {
  return (
    $0
  );
}

export default $1;
